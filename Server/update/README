Start the Server:
1：install Node
2. copy and paste Server folder to your machine
3. run npm install
4. start the server by typing node app


About the updata folder:
it contatins some changes to the Server, you can replace original files in Server folder with files in update folder.



